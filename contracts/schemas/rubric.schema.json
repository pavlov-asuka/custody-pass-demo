{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://custody-training.local/schemas/rubric.schema.json",
  "title": "后端私有评分资产",
  "type": "object",
  "additionalProperties": false,
  "required": ["routeId", "rubricVersion", "totalScore", "passScore", "dimensions", "mandatoryRequirements", "remediationTargets", "referenceAnswer"],
  "properties": {
    "routeId": {"type": "string"},
    "rubricVersion": {"type": "string"},
    "totalScore": {"const": 100},
    "passScore": {"const": 75},
    "dimensions": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["dimension", "maxScore", "criteria"],
        "properties": {
          "dimension": {"enum": ["CONCEPT", "PROCESS", "RISK", "EXPRESSION"]},
          "maxScore": {"type": "integer"},
          "criteria": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["criterionId", "description", "weight", "evidenceRequirement", "evidenceRules", "remediationTargetId"],
              "properties": {
                "criterionId": {"type": "string"},
                "description": {"type": "string"},
                "weight": {"type": "integer", "minimum": 1},
                "evidenceRequirement": {"type": "string"},
                "evidenceRules": {
                  "type": "array",
                  "minItems": 1,
                  "items": {"$ref": "#/$defs/evidenceRule"}
                },
                "remediationTargetId": {"type": "string"}
              }
            }
          }
        }
      }
    },
    "mandatoryRequirements": {
      "type": "array",
      "minItems": 1,
      "maxItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["requirementId", "description", "evidenceRequirement", "evidenceRules", "remediationTargetId"],
        "properties": {
          "requirementId": {"type": "string"},
          "description": {"type": "string"},
          "evidenceRequirement": {"type": "string"},
          "evidenceRules": {
            "type": "array",
            "minItems": 1,
            "items": {"$ref": "#/$defs/evidenceRule"}
          },
          "remediationTargetId": {"type": "string"}
        }
      }
    },
    "remediationTargets": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["targetId", "title", "reason", "materialStep", "materialItemId", "questionId"],
        "properties": {
          "targetId": {"type": "string"},
          "title": {"type": "string"},
          "reason": {"type": "string"},
          "materialStep": {"enum": ["KNOWLEDGE_CARD", "DEMONSTRATION"]},
          "materialItemId": {"type": "string"},
          "questionId": {"type": "string"}
        }
      }
    },
    "referenceAnswer": {"type": "object"}
  },
  "$defs": {
    "evidenceRule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fieldId", "operator", "expected"],
      "properties": {
        "fieldId": {"type": "string"},
        "operator": {"enum": ["EQUALS", "NUMBER_EQUALS", "CONTAINS_ALL"]},
        "expected": {
          "oneOf": [
            {"type": "string"},
            {"type": "number"},
            {"type": "array", "minItems": 1, "items": {"type": "string"}}
          ]
        },
        "tolerance": {"type": "number", "minimum": 0}
      }
    }
  }
}
