# 视觉源资产

阶段2只交付前端可消费的视觉源资产和制作约束，不向 `frontend/` 写入任何文件。

- `xiaotuo/`：小托形象基准、生产绘制规范、动作与三世界道具清单。
- `asset-manifest.json`：资产标识、版本、用途和文件校验值。

PNG 是身份与比例基准，不是前端组件。Kimi 应按 `xiaotuo/README.md` 输出透明背景的生产切图或 SVG，并保持纯色、粗轮廓、无渐变。
